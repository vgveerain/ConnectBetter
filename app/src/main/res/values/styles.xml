<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.AppTheme.Light.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="android:navigationBarColor">@android:color/background_light</item>
    </style>

    <style name="ConnectBetter" parent="AppTheme" >
        <item name="android:navigationBarColor">@color/navColor</item>
        <item name="colorPrimaryDark">@color/navColor</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
